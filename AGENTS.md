## JDK

Java 25. Path: `D:/Program Files/Eclipse Adoptium/graalvm-jdk-25.0.3+9.1` (`org.gradle.java.home` in `gradle.properties`).

## Agent skills

### Issue tracker

GitHub Issues on `pynickle/Echo-Seed`, via `gh`. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five roles, each label string equal to its name. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: root `CONTEXT.md` plus `docs/adr/`. See `docs/agents/domain.md`.
